/// {
///     "description": "functions in key",
///     "result": "nop",
///     "known": true
/// }

key item( .*, name() ) {
    .
}

match / {
    apply item("username") dump
}

match .* dump {
    .
}
