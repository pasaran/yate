/// {
///     description: 'recursive function inside template body #1',
///     result: 24,
///     known: true
/// }

match / {
    func fact(n) {
        if n == 1 { 1 } else { n * fact(n - 1) }
    }

    fact(4)
}

