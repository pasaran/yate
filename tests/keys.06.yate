/// {
///     "description": "match multiple values while selecting by key",
///     "result": "firstsecond",
///     "known": "true"
/// }

key items( /.items.item, .id) { . }

match / {
    apply items(/.selected) dump
}

match .* dump {
   .id
}

