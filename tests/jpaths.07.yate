/// {
///     "description": "index() and count() in predicate",
///     "yate": "jpaths.07.yate",
///     "result": "01234"
/// }

match / {
    apply .label[ index() < count() / 2 ]
}

match .label {
    index()
}

