#   {
#       "description": "apply ..... #name",
#       "yate": "jpaths.06.yate",
#       "result": "blocksappcontentuserleft"
#   }

match / {
    apply .blocks.app.content.user.left
}

match .left {
    apply ..... name
    apply .... name
    apply ... name
    apply .. name
    apply . name
}

match .* name {
    name()
}

