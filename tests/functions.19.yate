/// {
///     "description": "Аргумент функции в предикате",
///     "result": "Second",
///     "known": true
/// }

func get(id) {
    /.items.item[.id == id]
}

match / {
    get("second").title
}
