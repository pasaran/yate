match / {
    class = apply . class
    title = apply . title
    <div class="{ class }" title="{ title }">
        "Hello"
    </div>
}

match / class {
    "b-hello"
}

match / title {
    'I &hearts; NY, SF & "Moscow"'
    .title
}

