match / {
    a = {
        "title": "Hello, World"
        "item": [
            for .item {
                .text
            }
        ]
    }

    foo(a)
}

func foo(nodeset a) {
    <h1>{ a.title }</h1>
    <ul>
        for a.item {
            <li>{ . }</li>
        }
    </ul>
}

