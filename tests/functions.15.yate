/// {
///     "description": "functions with the nodeset param",
///     "result": "zeroonetwo"
/// }

func three(nodeset labels) {
    apply labels[ index() < 3 ]
}

match / {
    three( .label )
}

match .label {
    .
}

